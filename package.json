{
  "name": "safe-eks-mcp",
  "version": "0.1.0",
  "description": "Readonly-first MCP server and OpenCode customization for safe Amazon EKS operations.",
  "type": "module",
  "bin": {
    "safe-eks-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
