# kubernetes-mcp

MVP placeholder for a Kubernetes MCP server.

Planned capabilities:

- List namespaces, pods, deployments, services, events, ingress, and HPA resources
- Read rollout status and pod restart information
- Start with namespace-scoped read-only permissions
- Avoid write operations until explicit approval workflows are implemented
