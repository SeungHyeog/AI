# grafana-mcp

MVP placeholder for a Grafana and Prometheus MCP server.

Planned capabilities:

- Query dashboards, panels, alerts, and datasource metadata
- Query Prometheus metrics for latency, error rate, restart count, and saturation
- Return links to relevant dashboards and panels for user verification
